import { useState } from "react"

function IndexPopup() {
  const [data, setData] = useState("")

  return (
    <div
      style={{
        padding: 16
      }}>
      <h2>Blackboard sucks</h2>
    </div>
  )
}

export default IndexPopup
